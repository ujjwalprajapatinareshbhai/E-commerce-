
use e_commerce;


CREATE TABLE customers (
    customer_id INT PRIMARY KEY,
    name VARCHAR(50),
    city VARCHAR(50)
);

CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    order_date DATE,
    amount INT,
    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
);

CREATE TABLE products (
    product_id INT PRIMARY KEY,
    product_name VARCHAR(50),
    category VARCHAR(50),
    price INT
);

CREATE TABLE order_details (
    order_id INT,
    product_id INT,
    quantity INT,
    PRIMARY KEY (order_id, product_id),
    FOREIGN KEY (order_id) REFERENCES orders(order_id),
    FOREIGN KEY (product_id) REFERENCES products(product_id)
);


-- INSERT INTO products VALUES
-- (1, 'Laptop', 'Electronics', 50000),
-- (2, 'Phone', 'Electronics', 20000),
-- (3, 'Shoes', 'Fashion', 3000),
-- (4, 'Watch', 'Accessories', 5000),
-- (5, 'Tablet', 'Electronics', 30000),
-- (6, 'Headphones', 'Electronics', 2000);


-- INSERT INTO customers (customer_id, name, city)
-- WITH RECURSIVE cte AS (
--     SELECT 1 AS n
--     UNION ALL
--     SELECT n + 1 FROM cte WHERE n < 1000
-- )
-- SELECT 
--     n,
--     CONCAT('Customer_', n),
--     CASE 
--         WHEN n % 5 = 0 THEN 'Delhi'
--         WHEN n % 5 = 1 THEN 'Mumbai'
--         WHEN n % 5 = 2 THEN 'Ahmedabad'
--         WHEN n % 5 = 3 THEN 'Bangalore'
--         ELSE 'Pune'
--     END
-- FROM cte;

-- SET SESSION cte_max_recursion_depth = 3000;

-- INSERT INTO orders (order_id, customer_id, order_date, amount)
-- WITH RECURSIVE cte AS (
--     SELECT 1 AS n
--     UNION ALL
--     SELECT n + 1 FROM cte WHERE n < 2000
-- )
-- SELECT 
--     n,
--     (n % 1000) + 1,
--     DATE_ADD('2023-01-01', INTERVAL (n % 365) DAY),
--     FLOOR(1000 + (RAND() * 9000))
-- FROM cte;

-- INSERT INTO order_details
-- SELECT 
--     o.order_id,
--     FLOOR(1 + RAND() * 6),
--     FLOOR(1 + RAND() * 3)
-- FROM orders o;

-- select * from customers;

-- Q1 : Total number of count

-- select count(*) as total_customer
-- from customers;

-- Q2 :Total number of orders

-- select count(*) as total_orders
-- from orders;

-- Q3 : Total revenue generated

-- select sum(amount) as total_revenue
-- from orders;

-- Q4 : Top 5 highest order

-- select * from orders
-- order by amount desc
-- limit 5;

-- Q5 : List all cities from customer (unique)

-- select distinct city from customers;

-- Q6 : Total spending per customer

-- select customer_id,
-- sum(amount) as total_spent
-- from orders
-- group by customer_id
-- order by total_spent desc;

-- Q7 : Top 5 customer by spending

-- select customer_id,
-- sum(amount) as total_spending
-- from orders
-- group by customer_id
-- order by total_spending desc
-- limit 5;

-- Q8: Revenue by city

-- select c.city,
-- sum(amount) as total_revenue
-- from orders o
-- join customers c
-- on c.customer_id = o.customer_id
-- group by c.city
-- order by total_revenue desc;


-- Q9 : Average order per customer

-- select customer_id,
-- avg(amount) as avg_order
-- from orders
-- group by customer_id;

-- Q10 : order above average amount

-- select *
-- from orders where
-- amount >(select avg(amount) from orders);

-- Q11: Rank customer by total spending

-- select 
-- customer_id,
-- sum(amount) as total_spent,
-- rank() over (order by sum(amount) desc) as ranking
-- from orders
-- group by customer_id

-- Q12 : Dense Rank hande ties

-- SELECT 
--     customer_id,
--     SUM(amount) AS total_spent,
--     DENSE_RANK() OVER (ORDER BY SUM(amount) DESC) AS ranked
-- FROM orders
-- GROUP BY customer_id;

-- Q13: Top orders for each customer

-- select * 
-- from (
--    select *,
--       row_number() over (partition by customer_id order by amount desc) as rn
--       from orders) t
--       where rn = 1;

-- Q14: running total sales

select 
order_date,
sum(amount) as daily_sales,
sum(sum(amount)) over (order by  order_date) as running_total
from orders
group by order_date; 

-- Q15: Previous order amount

-- select 
-- order_id,
-- customer_id,
-- amount,
-- lag(amount) over (partition by customer_id order by order_date) as prec_order
-- from orders;

-- Q16: Diff in current and previous order

-- select 
-- customer_id,
-- order_id,
-- amount,
-- amount - Lag(amount) over (partition by customer_id order by order_date) as diff
-- from orders;

-- Q17 : Monthly revenue trend

-- select 
-- month(order_date) as month,
-- sum(amount) as revenue
-- from orders
-- group by month(order_date)
-- order by month;

-- Q18 : Top 3 customer per city

-- select*
-- from(
-- select  o.customer_id,
-- c.city,
-- sum(amount) as total_spent,
-- rank() over (partition by c.city order by sum(o.amount) desc) as rnk
-- from orders o 
-- join customers c on o.customer_id = c.customer_id
-- group by c.city,o.customer_id
-- )t
-- where rnk <= 3

-- Q19 : Percentage contribution of each customer

-- select 
-- customer_id,
-- sum(amount) as total_spent,
-- (sum(amount) * 100 / sum(sum(amount)) over ()) as percentage
-- from orders
-- group by customer_id
-- order by percentage desc;

-- Q20: Customer who never placed an order

-- select * from 
-- customers where 
-- customer_id  not in (select customer_id from orders);

-- select c.*
--  from customers c
--  left join orders o
--  on c.customer_id = o.customer_id
--  where o.customer_id is null;


-- Q21: Customer who placed more than 5 order

-- select customer_id,count(*) as tottal_orders
-- from orders 
-- group by customer_id
-- having count(*) >5;

-- Q22 : Avg order per city

-- select c.city,avg(o.amount) as avg_amount
-- from orders o
-- join customers c
-- on o.customer_id = c.customer_id
-- group by c.city;

-- Q23: Highest order in city

-- select * from
--  (select c.city,
-- o.*,
-- row_number() over (partition by c.city order by o.amount desc) as rn
-- from orders o
-- join customers c
-- on o.customer_id = c.customer_id)t
-- where rn = 1;
																									
-- Q24 : Toatal number of product sold

-- select sum(quantity) as total_quantity from 
-- order_details; 

-- Q25: MOst sold product

-- select o.product_id,p.product_name, sum(quantity) as total_sold
-- from order_details o
-- join products p
-- on o.product_id = p.product_id
-- group by product_id
-- order by total_sold desc
-- limit 1;                                                                                          

--                                                                                                     
-- Q26 : Top 3 order per customer

-- select * from 
-- (select *,
-- row_number() over (partition by customer_id order by amount desc) as rn
-- from orders)t
-- where rn <= 3;

-- Q27 : Find running total per customer

-- select 
-- customer_id,
-- order_id,
-- amount,
-- order_date,
-- sum(amount) over (partition by customer_id order by order_date) as running_total
-- from orders;

-- Q28: Diff between current and next order

-- select 
-- order_id,
-- customer_id,
-- amount,
-- order_date,
-- lead(amount) over (partition by customer_id order by order_date) as next_order
-- from orders;

-- Q29 : find the customer whose latest order is greater than previous order

-- select *
-- from (select 
-- customer_id,
-- order_id,
-- amount,
-- order_date,
-- lag(amount) over( partition by customer_id order by order_date) as prev_order
-- from orders)t
-- where prev_order is not null and amount > prev_order

select * from products;

